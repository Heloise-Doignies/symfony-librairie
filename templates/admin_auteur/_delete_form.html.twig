<form method="post" action="{{ path('app_admin_auteur_delete', {'id': auteur.id}) }}" onsubmit="return confirm(''Êtes-vous sûr.es de vouloir supprimer ce livre ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ auteur.id) }}">
    <button class="btn btn-danger"><i class="align-middle" data-feather="trash-2"></i></button>
</form>
